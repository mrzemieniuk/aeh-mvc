{% extends 'base.html.twig' %}

{% block title %}New EmergencyAlert{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="card">
            <div class="card-header bg-primary text-white">
                <h1 class="mb-0">Create new EmergencyAlert</h1>
            </div>
            <div class="card-body">
                {{ include('admin/emergency_alert/_form.html.twig') }}
            </div>
            <div class="card-footer d-flex justify-content-between">
                <a class="btn btn-secondary" href="{{ path('app_emergency_alert_index') }}">Back to list</a>
            </div>
        </div>
    </div>
{% endblock %}
